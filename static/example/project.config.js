export default {
  defaultLanguage: "cpp",
  templates: ["cpp"], // will generate selected language templates
  homeworkList: [
    {
      A: {
        // if not defined, use defaultLanguage
        // language: "cpp",
        sampleInput: "1234",
        sampleOutput: "123"
      }
    }
  ]
};
